<resources>
    <!-- App Main Theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/orange</item>
        <!-- Use white icons/text on status bar -->
        <item name="android:windowLightStatusBar">false</item>
        <!-- Optional: remove ActionBar for all activities -->
    </style>

    <!-- Existing TimeSlotButton style -->
    <style name="TimeSlotButton">
        <item name="android:background">@drawable/btn_time_slot_bg</item>
        <item name="android:textColor">#E87C00</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
